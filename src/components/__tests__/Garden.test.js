import {render} from '@testing-library/react'
import Garden from '../Garden'
import React from 'react'

test('returns an empty array for plants', () => {   
    
    // expect(Garden.emptyPlantsArray(30, 30)).toHaveLength(1)
    // expect(Garden.emptyPlantsArray(40, 40)).toHaveLength(4)
    // expect(Garden.emptyPlantsArray(111, 111)).toHaveLength(25)
    // expect(Garden.emptyPlantsArray(99, 111)).toHaveLength(20)
    // expect(Garden.emptyPlantsArray(99, 119)).toHaveLength(20)
    // expect(Garden.emptyPlantsArray(300, 300)).toHaveLength(225)
   
 });


